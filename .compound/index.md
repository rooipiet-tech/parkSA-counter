L-0001 [conventions] Touch: commit on pointerup+slop, not pointerdown, or scroll-from-tile becomes a phantom tap; only real CDP touch-swipe tests catch it (active)
L-0002 [architecture] A path called only by unit tests that new the class can be unreachable at runtime; require a UI/API caller + e2e via the real surface (active)
L-0003 [conventions] Sort-then-assert-sorted (transform == assertion) is a vacuous test; assert against independently recorded ground truth (active)
L-0004 [architecture] Offline queue: delete-pending-by-key after ack is a lost-update race; use compare-and-delete + a mid-flush mutation test (active)
L-0005 [security] CSV export of user/remote-writable text is formula injection (=+-@); prefix-guard + check who can write the source field (active)
L-0006 [performance] PostgREST select() caps at 1000 rows; unpaginated reads feeding dashboards/export silently truncate; paginate or filter server-side; test >1000 rows (active)
L-0007 [domain-constraints] Offline cross-tz: single local-zone binning choke point + aggregate by client device_ts with client-written offline flag; UTC tests hide both bugs (active)
L-0008 [data-residency] No in-jurisdiction backend region: store NO personal info (moots cross-border) via validated pseudonymous codes + ban identifying captures; watch permissive regex (active)
L-0009 [security] Destructive dev/test wipe hooks must be DEV-gated, never in prod, and refuse when unsynced data exists; test the hook is inert in prod (active)
